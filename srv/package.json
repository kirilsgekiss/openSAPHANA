{
    "name": "openSAPHANA-srv",
    "description": "Generated from ../package.json, do not change!",
    "version": "1.0.0",
    "dependencies": {
        "@sap/cds": "^2.10",
        "express": "^4.17.1",
        "@sap/hana-client": "^2.4.196"
    },
    "engines": {
        "node": ">=4.0.0 <9.0.0"
    },
    "devDependencies": { "sqlite3": "*" },
    "scripts": {
    	"postinstall": "npm dedupe && node .build.js && cds build/all --project .. --clean",
        "start": "node ./node_modules/@sap/cds/bin/cds.js serve gen/csn.json",
        "watch": "nodemon -w . -i node_modules/**,.git/** -e cds -x npm run build"
    },
    "private": true,
    "cds": {
        "requires": {
            "db": {
                "kind": "hana",
                "model": "gen/csn.json"
            }
        }
    }
}
